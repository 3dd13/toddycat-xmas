<p id="notice"><%= notice %></p>

<p>
  <strong>Bookshelf:</strong>
  <%= @book.bookshelf.name %>
</p>

<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>

<%= link_to 'Edit', edit_bookshelf_book_path(@bookshelf, @book), :class => "btn btn-primary" %> |
<%= link_to 'Back', bookshelf_books_path(@bookshelf) %>
