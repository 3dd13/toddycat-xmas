<%= form_for(@bookshelf) do |f| %>
  <% if @bookshelf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookshelf.errors.count, "error") %> prohibited this bookshelf from being saved:</h2>

      <ul>
      <% @bookshelf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <%= f.fields_for :books, @bookshelf.books do |book_form| %>
    <div class="field">
      <%= book_form.label :title, "Book Title" %><br>
      <%= book_form.text_field :title %>
    </div>
  <% end %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
